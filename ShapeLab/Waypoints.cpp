#include "Waypoints.h"
#include "MainWindow.h"
#include "ui_MainWindow.h"

void waypoints::translation() 
{
	/*initWayPoint.resize(10, 8);
	for (int i = 0; i < 10; i++)
	{
		for (int j = 0; j < 8; j++)
			initWayPoint(i,j) = i * j;
		
	}
	cout << initWayPoint << endl;
	cout << initWayPoint.col(0) << endl;*/

	cout <<
}